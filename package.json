{
  "name": "github-user-search",
  "version": "1.0.0",
  "description": "Building up a small webapp to search every github user by their usernames",
  "main": "tailwind.config.js",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./dist/style.css --minify",
    "watch": "tailwindcss -i ./input.css -o ./dist/style.css --watch"
  },
  "author": "Bon Tertius Tuyishimire",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.27",
    "tailwindcss": "^3.3.3"
  }
}
